import { NextResponse } from 'next/server'; import type { NextRequest } from 'next/server'; export function middleware(request:NextRequest){ const url=new URL(request.url); const lower=url.pathname.toLowerCase(); if(url.pathname!==lower){ url.pathname=lower; return NextResponse.redirect(url.toString(),{status:308}); } return NextResponse.next(); } export const config={ matcher:['/:path*'] };
